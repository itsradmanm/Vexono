# راهنمای مشارکت در پروژه Vexono (Contributing Guide)

از این که می‌خواهید در توسعه و بهبود اپلیکیشن **Vexono** مشارکت کنید بسیار متشکریم!

---

## 🛠 فرآیند ارسال تغییرات (Workflow)

۱. **فورک کردن مخزن:**
   - مخزن را فورک کرده و بر روی سیستم خود کلون کنید.

۲. **ایجاد برنچ جدید:**
   - برای قابلیت‌های جدید از برنچ‌های `feature/feature-name` استفاده کنید.
   - برای رفع باگ‌ها از برنچ‌های `fix/bug-name` استفاده کنید.

۳. **رعایت استانداردهای کدنویسی:**
   - کد را ۱۰۰٪ با Kotlin و بدون XML Layout (فقط Jetpack Compose) بنویسید.
   - معماری Clean Architecture و MVVM را حفظ کنید.
   - برای پیام‌های کامیت از فرمت [Conventional Commits](https://www.conventionalcommits.org/) استفاده کنید (`feat:`, `fix:`, `docs:`, `refactor:`, `test:`).

۴. **تست و اعتبارسنجی:**
   - قبل از ثبت PR، مطمئن شوید تمام تست‌ها با موفقیت پاس می‌شوند:
   ```bash
   ./gradlew test
   ./gradlew lint
   ```

۵. **ارسال Pull Request:**
   - توضیحات شفاف درباره تغییرات اعمال شده ارائه دهید.
